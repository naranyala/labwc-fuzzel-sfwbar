# Plan: Break Down main_shell.zig into Modules (Incremental)

## Current State
- `zigshell-cairo-pango/src/main_shell.zig`: **2,370 lines**
- `zigshell-blend2d/src/main_shell.zig`: **1,624 lines**

## Strategy: Extract in Phases

### Phase 1: Create `wayland_core.zig` (~300 lines) — DONE
Extract: Wayland globals, toplevel callbacks, registry, output tracking.
Status: File created, needs integration.

### Phase 2: Create `settings.zig` (~400 lines)
Extract: Settings state, geometry, config sync, UI drawing.
Dependencies: panel_mod, dock_mod, icon, pcfg.

### Phase 3: Create `session.zig` (~100 lines)
Extract: Session popup state, drawing, click handling.

### Phase 4: Create `modal.zig` (~200 lines)
Extract: Modal state, calendar, rendering.

### Phase 5: Create `dock_view.zig` (~150 lines)
Extract: Dock rendering, tooltip.

### Phase 6: Create `launcher.zig` (~100 lines)
Extract: App launcher state, toggle, rendering.

### Phase 7: Slim `main_shell.zig` (~300 lines)
Keep: Main function, event loop, cleanup.
Import all modules.

### Phase 8: Apply same breakdown to blend2d variant
Port modules to use Blend2D renderer instead of Cairo.

---

## File Structure (target)

```
src/shells/zigshell-cairo-pango/src/
├── main_shell.zig        # ~300 lines (main + event loop)
├── wayland_core.zig      # ~300 lines (Wayland globals + callbacks)
├── settings.zig          # ~400 lines (settings UI + config)
├── session.zig           # ~100 lines (session popup)
├── modal.zig             # ~200 lines (modal dialog)
├── dock_view.zig         # ~150 lines (dock rendering)
├── launcher.zig          # ~100 lines (app launcher)
├── panel.zig             # (existing)
├── dock.zig              # (existing)
├── icon.zig              # (existing)
├── theme.zig             # (existing)
├── panel_config.zig      # (existing)
├── c.zig                 # (existing)
└── build.zig             # (existing, updated)
```

## Next Steps
1. Integrate `wayland_core.zig` into main_shell.zig
2. Create `settings.zig` module
3. Create remaining modules
4. Test and verify
5. Apply to blend2d variant
